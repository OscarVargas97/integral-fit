const NotAuthMiddleware = (supabaseClient, request) => {
  // if (
  //  !isInAuthPath(request.nextUrl.pathname) &&
  //  request.nextUrl.pathname !== '/'
  // ) {
  //  return redirectToHome(request)
  // }
}

export default NotAuthMiddleware
